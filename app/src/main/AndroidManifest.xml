<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.medicalLink.muslim.mldoctorapp">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/logo"
        android:supportsRtl="false"
        android:theme="@style/AppTheme">
        <activity android:name="com.medicalLink.muslim.mldoctorapp.signin">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.medicalLink.muslim.mldoctorapp.appointment" />
        <activity android:name="com.medicalLink.muslim.mldoctorapp.profile" />
        <activity android:name="com.medicalLink.muslim.mldoctorapp.repeataion" />
        <activity android:name="com.medicalLink.muslim.mldoctorapp.visitors" />
        <activity android:name="com.medicalLink.muslim.mldoctorapp.MainActivity"></activity>
    </application>

</manifest>